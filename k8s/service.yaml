apiVersion: v1
kind: Service
metadata:
  name: naki-system-service
  namespace: naki
spec:
  selector:
    app: naki-system-service
  ports:
    - name: http
      port: 80
      targetPort: 5880
      protocol: TCP
  type: ClusterIP
